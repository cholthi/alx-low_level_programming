#include "main.h"
/* more headers goes there */

/**
* _isdigit - checks its argument if it is a numeric digit character
* @c: Int char to check
*
* Description: Check argument if numeric digit
* Return: int bool
*/
int _isdigit(int c)
{
	return ((c >= 48 && c <= 57));
}

