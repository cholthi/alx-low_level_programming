#include "main.h"
/* more headers goes there */

/**
* mul - checks its argument if it is a uppercase character
* @a: Int char to check
* @b: Int argment to mult
*
* Description: Check argument if uppercase
* Return: int result
*/
int mul(int a, int b)
{
	long int r;

	r = a * b;
	return (r);
}

