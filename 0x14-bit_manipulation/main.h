#ifndef MAIN_FUNCTIONS_H
#define MAIN_FUNCTIONS_H

/**
 * Desc: Main header file for function and tyoe declarations
 */
#include <stdlib.h>

unsigned int binary_to_uint(const char *b);
int is_binary(const char *);

#endif
